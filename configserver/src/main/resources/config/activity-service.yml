spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      username: root
      password: password
      database: activitydb
      authentication-database: admin

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

server:
  port: 8082

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

rabbitmq:
  exchange:
    name: fittness.exchange
  queue:
    name: activity.queue
  routing:
    key: activity.routing
